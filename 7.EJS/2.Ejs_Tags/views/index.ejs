<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ejs</title>
</head>
<body>
    <h1><%= title %></h1>
    <p>Current Second is: <%= seconds %></p>

    <% if(seconds % 2 === 0) { %>
        <ul>
            <% for(let i = 0; i < items.length; i++) { %>
                <li>
                    <%= items[i] %>
                </li>
            <% } %>
        </ul>
    <% } else { %>
        <p>No item to display. </p>
    <% } %>

    <!-- html content: -->
     <p>
        <%- htmlContent %>
     </p>

     <!-- use another file inside this file -->
      <%- include("footer.ejs") %>
</body>
</html>